name: 🐕 Watchdog Cron

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping-watchdog:
    runs-on: ubuntu-latest
    steps:
      - name: Log timestamp
        run: date
      - name: Call my Render API
        run: curl -I https://ticky-api.onrender.com
